export type RecentConnection = {
  host: string;
  port: number;
  username: string;
  lastUsedAt: number;
};

export type ScreenSource = {
  id: string;
  name: string;
  thumbnailDataUrl: string;
};
